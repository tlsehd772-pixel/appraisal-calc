import streamlit as st
import datetime

# --- 페이지 기본 설정 ---
st.set_page_config(page_title="감정평가 실무/이론 계산기", layout="wide")

# --- 스타일링 (가독성 향상) ---
st.markdown("""
    <style>
    .big-font { font-size:20px !important; font-weight: bold; }
    .info-box { background-color: #e8f4f8; padding: 15px; border-radius: 10px; border-left: 5px solid #0078d7; }
    .warning-box { background-color: #fff3cd; padding: 15px; border-radius: 10px; border-left: 5px solid #ffc107; }
    </style>
    """, unsafe_allow_html=True)

st.title("🧮 감정평가사 수험/실무 통합 계산기")
st.markdown("수험생: 신현동 | 목표: 감정평가사 합격")
st.markdown("---")

# --- 사이드바: 평가 방식 선택 ---
menu = st.sidebar.selectbox(
    "평가 대상을 선택하세요",
    [
        "🏠 홈 / 평가 가이드 (분류표)",
        "📋 산정 요인 및 고려사항 가이드",   # 👈 새로 추가됨 (기본 화면)
        "토지 (공시지가기준법)", 
        "건물 (원가법)", 
        "토지건물 일괄/구분 (거래사례비교법)",
        "수익성 부동산 (수익환원법)",
        "투자타당성/가치 (할인현금수지분석법 DCF)",
        "임대료 (적산법)",
        "임대료 (임대사례비교법)",
        "토지/건물 가치 분리 (잔여법 & 투자결합법)",
        "공공사업 보상 (보상평가)",
        "재개발/재건축 (정비사업평가)"
    ]
)
# ==============================================================================
# 0. 홈 / 평가 가이드 (Dashboard)
# ==============================================================================
if menu == "🏠 홈 / 평가 가이드 (분류표)":
    st.header("👋 감정평가사 실무 가이드")
    st.markdown("### 💡 무엇을 평가하시나요? (감정평가규칙 제14조~25조)")
    st.info("물건의 종류에 따라 적용해야 할 **'주된 평가방법'**이 법으로 정해져 있습니다. 아래 표를 보고 적절한 메뉴를 선택하세요.")

    # 탭으로 보기 편하게 구분
    tab1, tab2, tab3, tab4 = st.tabs(["🏗️ 유형자산 (토지/건물)", "🚗 동산/기타자산", "💼 권리/기업가치", "⚖️ 목적별 비교 (담보/보상/정비)"])

    with tab1:
        st.subheader("토지 및 건물 평가")
        st.markdown("""
        | 평가 대상 | 주된 평가방법 (원칙) | **감가수정 (주방법)** | **합리성 검토 (부방법)** | 메뉴 선택 |
        | :--- | :--- | :--- | :--- | :--- |
        | **토지** | **공시지가기준법** | - | 거래사례비교법 (그밖의요인) | 👉 `토지 (공시지가기준법)` |
        | **건물** | **원가법** | **정액법** (내용연수법) | **관찰감가법** | 👉 `건물 (원가법)` |
        | **집합건물** | **거래사례비교법** | - | 원가법 (층별/호별효용) | 👉 `일괄/구분 (거래사례)` |
        | **과수원** | **거래사례비교법** | - | 원가법 (수목 가격) | 👉 `일괄/구분 (거래사례)` |
        """)
        st.warning("⚠️ **주의:** 토지도 '적정한 실거래가'가 있다면 거래사례비교법을 적용할 수 있음.")
        st.info("💡 **암기 Tip:** 건물은 내용연수 동안 가치가 일정하게 떨어진다고 가정하는 **'정액법'**이 원칙이자 실무 관행입니다.")
        st.info("""
        💡 **실무 Point (건물 감가):** 이론상 건물은 '정액법'을 적용하지만, 실제 관리 상태나 개보수 여부를 반영하기 위해 반드시 **[관찰감가법]**을 병용하여 감가액을 조정(합리성 검토)해야 합니다.
        """)

    with tab2:
        st.subheader("동산 및 기타자산")
        st.markdown("""
        | 평가 대상 | 주된 평가방법 (원칙) | **감가수정 (주방법)** | **합리성 검토 (부방법)** | 메뉴 선택 |
        | :--- | :--- | :--- | :--- | :--- |
        | **건설기계** | **원가법** | **정률법** | **관찰감가법** | 👉 `건물 (원가법)` |
        | **선박** | **원가법** | **정액법** (선체) | **관찰감가법** | 👉 `건물 (원가법)` |
        | **항공기** | **원가법** | **정액법** | **관찰감가법** | 👉 `건물 (원가법)` |
        | **자동차** | **거래사례비교법** | - | 원가법 (참고) | 👉 `일괄/구분 (거래사례)` |
        | **기계기구** | **원가법** | **정률법** | **관찰감가법** | 👉 `건물 (원가법)` |
        """)
        st.warning("⚠️ **시험 포인트:** 내용연수법(수학적 계산)만으로는 개별적인 상태를 반영 못하므로, 실지조사를 통한 **관찰감가법 병용**이 필수적입니다.")
        st.caption("※ **꿀팁:** 자동차는 거래사례비교법이지만, 건설기계는 원가법입니다. (시험 단골 함정!)")
        st.warning("⚠️ **시험 포인트:** 건물은 정액법이지만, **기계기구(건설기계 포함)는 '정률법'**을 적용하여 초기에 많이 감가하는 것이 원칙입니다.")

    with tab3:
        st.subheader("무형자산 및 임대료")
        st.markdown("""
        | 평가 대상 | 주된 평가방법 (원칙) | **상각/환원 (주방법)** | **합리성 검토 (부방법)** | 메뉴 선택 |
        | :--- | :--- | :--- | :--- | :--- |
        | **광업재단** | **수익환원법** | **생산량비례법** | - | 👉 `수익성 (수익환원법)` |
        | **영업권** | **수익환원법** | **인우드법** | 거래사례비교법 | 👉 `투자타당성 (DCF)` |
        | **임대료** | **임대사례비교법** | - | **적산법** | 👉 `임대료 (임대사례)` |
        | **기업가치** | **수익환원법** | DCF | 각종 비율분석 | 👉 `투자타당성 (DCF)` |
        """)
        st.caption("※ **임대료:** 임대사례비교법이 곤란한 경우 적산법을 쓰거나, 적산법으로 구한 시산임료로 합리성을 검토합니다.")
        st.caption("※ 광산 등 고갈성 자산은 기간보다는 **'캐낸 양(생산량)'**에 비례해서 감가합니다.")

    # --- [New] 목적별 비교 탭 ---
    with tab4:
        st.subheader("⚖️ 평가 목적별 핵심 기준 비교")
        st.markdown("실무와 이론 시험에서 가장 중요한 **4대 평가 목적**의 차이점입니다.")
        
        # 가독성을 위한 스타일 적용된 표
        st.markdown("""
        | 구분 | **일반 평가** (매매 등) | **담보 평가** | **보상 평가** (경매/공매 포함) | **정비사업 평가** (재개발) |
        | :--- | :--- | :--- | :--- | :--- |
        | **근거 법령** | 감정평가법 / 감칙 | 은행업감독규정 / 감칙 | **토지보상법** | **도시정비법** |
        | **평가 가액** | **시장가치** (Market Value) | **시장가치** (보수적) | **적정가격** (Proper Price) | **종전자산 평가액** |
        | **기준 시점** | 가격조사 완료일 | 가격조사 완료일 | 협의/재결 당시 | **사업시행인가 고시일** |
        | **표준지 선정** | 기준시점 당해년도 | 기준시점 당해년도 | **사업인정고시일 전** 공시지가 | 기준시점 당해년도 |
        | **개발이익** | 반영함 (현황 기준) | 반영함 (환가성 고려) | **철저히 배제** | 반영함 (개발 기대감 포함) |
        """)

        st.divider()
        
        col_a, col_b = st.columns(2)
        with col_a:
            st.error("🛑 **보상평가 주의사항 (토지보상법)**")
            st.markdown("""
            * **표준지:** 반드시 '사업인정고시일' 이전의 공시지가를 써야 함. (해당 사업으로 오른 땅값 배제)
            * **시점수정:** 해당 사업과 관계없는 인근 시/군/구 지가변동률 사용.
            * **그 밖의 요인:** 보상 선례 참작 시에도 개발이익이 포함되지 않은 사례를 써야 함.
            """)
            
        with col_b:
            st.warning("🏗️ **정비사업 평가 주의사항 (도정법)**")
            st.markdown("""
            * **기준시점:** 조합원 분담금을 정하는 '관리처분계획' 수립을 위한 평가는 **사업시행인가 고시일**이 기준임.
            * **목적:** 조합원 간의 형평성(비례율)을 맞추는 것이 핵심이므로, 절대적 가격보다 상대적 가격 균형이 중요함.
            """)
            
        st.info("🏦 **담보평가 Tip:** 담보평가는 은행이 돈을 떼이지 않게 하는 것이 목적이므로, 나중에 경매로 넘어갔을 때 팔릴 가격(낙찰가율)을 고려하여 **보수적으로 평가**하는 경향이 있습니다.")
    st.divider()
    st.subheader("📌 감정평가 3방식 6방법 요약")
    
    col_a, col_b, col_c = st.columns(3)
    with col_a:
        st.error("**비교방식 (시장성)**")
        st.write("1. 거래사례비교법")
        st.write("2. 임대사례비교법")
        st.write("3. 공시지가기준법")
    with col_b:
        st.warning("**원가방식 (비용성)**")
        st.write("4. 원가법")
        st.write("5. 적산법")
    with col_c:
        st.success("**수익방식 (수익성)**")
        st.write("6. 수익환원법")
        st.write("7. 수익분석법")
# ==============================================================================
# 1. 토지 평가 (공시지가기준법)
# ==============================================================================
if menu == "토지 (공시지가기준법)":
    st.header("📍 토지 평가: 공시지가기준법")
    
    col_input, col_result = st.columns([1, 1.2])

    with col_input:
        st.subheader("1. 요인 입력 (Inputs)")
        
        # 1) 표준지
        st.markdown("**① 표준지 공시지가 ($P_{std}$)**")
        p_std = st.number_input("표준지 단가 (원/㎡)", value=1000000, step=10000, format="%d")
        
        # 2) 시점수정
        st.markdown("**② 시점수정 ($Time$)**")
        time_mod = st.number_input("지가변동률 적용치 (1+r)", value=1.035, format="%.4f", step=0.001, help="기준시점까지의 지가변동률")
        
        # 3) 지역요인
        st.markdown("**③ 지역요인 ($Reg$)**")
        reg_factor = st.number_input("지역요인 비교치", value=1.000, format="%.3f", step=0.01, help="인근지역 소재 시 1.00")
        
        # 4) 개별요인
        st.markdown("**④ 개별요인 ($Ind$)**")
        ind_factor = st.number_input("개별요인 비교치", value=1.100, format="%.3f", step=0.01, help="가로, 접근, 환경, 획지 조건 등")
        
        # 5) 그 밖의 요인
        st.markdown("**⑤ 그 밖의 요인 ($Other$)**")
        other_factor = st.number_input("기타요인 보정치", value=1.500, format="%.3f", step=0.01, help="거래사례/평가선례 참작")
        
        # 6) 면적
        area = st.number_input("대상물건 면적 (㎡)", value=165.0, format="%.1f")

    with col_result:
        st.subheader("2. 계산 결과 및 풀이")
        
        # 계산 로직
        final_unit_price = p_std * time_mod * reg_factor * ind_factor * other_factor
        final_total_price = final_unit_price * area
        
        # 결과 표시
        st.success(f"🎯 ㎡당 단가: {int(final_unit_price):,} 원")
        st.error(f"💰 총 평가액: {int(final_total_price):,} 원")
        
        st.markdown("### 📝 수식 풀이 과정")
        st.latex(r'''
        P_{target} = P_{std} \times \text{시점} \times \text{지역} \times \text{개별} \times \text{그밖}
        ''')
        
        calculation_text = f"""
        {p_std:,} (표준지)
        x {time_mod:.4f} (시점수정)
        x {reg_factor:.3f} (지역요인)
        x {ind_factor:.3f} (개별요인)
        x {other_factor:.3f} (그 밖의 요인)
        ---------------------------
        = {int(final_unit_price):,} 원/㎡
        """
        st.code(calculation_text)

        # 하단 이론/법규 노트
        st.markdown("---")
        st.markdown('<div class="info-box">', unsafe_allow_html=True)
        st.markdown("#### 💡 핵심 Check Point")
        st.markdown("""
        * **[법규] 감칙 제14조:** 토지는 공시지가기준법이 원칙. 적정한 실거래가가 있는 경우 거래사례비교법 가능.
        * **[이론/실무] 사정보정 X:** 표준지 공시지가는 이미 적정 가격이므로 **사정보정(Speculation Check)을 절대 하지 않음.**
        * **[실무 Tip]** 그 밖의 요인 보정치는 인근 지역의 거래사례나 평가 선례를 역산하여 산출함(가장 배점이 높고 틀리기 쉬움).
        """)
        st.markdown('</div>', unsafe_allow_html=True)

# ==============================================================================
# 2. 건물 평가 (원가법 - 감가수정 방법별 비교)
# ==============================================================================
elif menu == "건물 (원가법)":
    st.header("🏗️ 건물 평가: 원가법 (감가수정 3방식)")
    st.markdown("내용연수에 의한 감가수정 방법(정액법, 정률법, 상환기금법) 비교")
    
    # 감가수정 방법 선택
    dep_method = st.radio(
        "감가수정 방법을 선택하세요",
        ["정액법 (Straight-line)", "정률법 (Declining Balance)", "상환기금법 (Sinking Fund)"],
        horizontal=True
    )

    col_input, col_result = st.columns([1, 1.2])
    
    with col_input:
        st.subheader("1. 입력 (Inputs)")
        reprocurement_cost = st.number_input("재조달원가 (C) (원)", value=500000000, step=1000000, format="%d")
        useful_life = st.number_input("전체 내용연수 (N년)", value=40, step=1)
        elapsed_years = st.number_input("경과 연수 (n년)", value=10, step=1)
        residual_ratio = st.slider("잔가율 (R) (%)", 0, 30, 10) / 100
        
        # 상환기금법일 때만 이자율 입력창이 뜨도록 설정
        safe_rate = 0.0
        if "상환기금법" in dep_method:
            st.markdown("##### ➕ 상환기금법 추가 입력")
            safe_rate = st.number_input("축적이율 (i) (%)", value=2.0, step=0.1, format="%.2f") / 100

    with col_result:
        st.subheader("2. 적산가액 산출")
        
        current_value = 0
        depreciation_total = 0
        
        # --- A. 정액법 계산 ---
        if "정액법" in dep_method:
            # 매년 감가액 = C * (1-R) / N
            annual_dep = reprocurement_cost * (1 - residual_ratio) / useful_life
            depreciation_total = annual_dep * elapsed_years
            current_value = reprocurement_cost - depreciation_total
            
            st.markdown("### 📝 수식 (정액법)")
            st.latex(r'''
            P = C - D_n = C - \left[ \underbrace{C(1-R)}_{\text{대상액}} \times \frac{1}{N} \times n \right]
            ''')
            st.info("매년 감가액이 일정함 (직선 형태)")

        # --- B. 정률법 계산 ---
        elif "정률법" in dep_method:
            # 잔가율 R = (1-d)^N  => 정률 d = 1 - R^(1/N)
            if residual_ratio <= 0:
                d = 0.9 # 잔가율 0이면 계산 불가하므로 임의 설정 (이론상 잔가율>0 필요)
                st.warning("정률법은 잔가율이 0보다 커야 정확합니다.")
            else:
                d = 1 - (residual_ratio ** (1/useful_life))
            
            # n년 후 가치 = C * (1-d)^n
            current_value = reprocurement_cost * ((1 - d) ** elapsed_years)
            depreciation_total = reprocurement_cost - current_value
            
            st.markdown(f"**산출된 상각률(d):** {d*100:.2f}%")
            st.markdown("### 📝 수식 (정률법)")
            st.latex(r'''
            P = C (1-d)^n \quad \left( \text{단,} \ d = 1 - \sqrt[N]{R} \right)
            ''')
            st.info("초기에 감가액이 크고 갈수록 줄어듦 (체감상각)")

        # --- C. 상환기금법 계산 ---
        elif "상환기금법" in dep_method:
            # 감가상각대상액 = C * (1-R)
            target_dep_amt = reprocurement_cost * (1 - residual_ratio)
            
            # 감채기금계수 (매년 적립액 비율)
            if safe_rate == 0:
                sff = 1/useful_life
            else:
                sff = safe_rate / ((1 + safe_rate) ** useful_life - 1)
                
            # n년 동안의 감가누계액 = 매년적립액 * 연금의 내가계수
            # 연금내가표 = ((1+i)^n - 1) / i
            if safe_rate == 0:
                future_value_factor = elapsed_years
            else:
                future_value_factor = ((1 + safe_rate) ** elapsed_years - 1) / safe_rate
            
            depreciation_total = (target_dep_amt * sff) * future_value_factor
            current_value = reprocurement_cost - depreciation_total
            
            st.markdown("### 📝 수식 (상환기금법)")
            st.latex(r'''
            D_n = \underbrace{C(1-R)}_{\text{대상액}} \times \underbrace{\frac{i}{(1+i)^N - 1}}_{\text{상환기금율(SFF)}} \times \underbrace{\frac{(1+i)^n - 1}{i}}_{\text{연금의내가계수}}
            ''')
            st.info("이자가 붙으므로 실제 적립 부담이 적음 (감가액이 적게 잡힘 → 가격이 높음)")

        # 결과 표시 (공통)
        st.divider()
        st.write(f"**감가누계액:** {int(depreciation_total):,} 원")
        st.success(f"🎯 적산가액: {int(current_value):,} 원")
        
        # --- [보너스] 그래프로 비교하기 ---
        st.markdown("---")
        st.subheader("📊 감가수정 곡선 비교 (Graph)")
        
        import pandas as pd
        import numpy as np
        
        # 0년 ~ N년까지 데이터 생성
        years = list(range(0, int(useful_life) + 1))
        
        # 1. 정액법 데이터
        val_straight = [reprocurement_cost * (1 - (1-residual_ratio)*(y/useful_life)) for y in years]
        
        # 2. 정률법 데이터
        d_rate = 1 - (residual_ratio ** (1/useful_life))
        val_declining = [reprocurement_cost * ((1-d_rate)**y) for y in years]
        
        # 3. 상환기금법 데이터 (이율 2% 가정)
        # 비교를 위해 고정이율 2% 사용 (사용자 입력값이 0일수도 있어서)
        i_graph = 0.02
        sff_graph = i_graph / ((1 + i_graph) ** useful_life - 1)
        target_amt = reprocurement_cost * (1 - residual_ratio)
        val_sinking = []
        for y in years:
            acc_factor = ((1 + i_graph) ** y - 1) / i_graph
            dep_amt = target_amt * sff_graph * acc_factor
            val_sinking.append(reprocurement_cost - dep_amt)

        chart_data = pd.DataFrame({
            '연수': years,
            '정액법': val_straight,
            '정률법': val_declining,
            '상환기금법(i=2%)': val_sinking
        }).set_index('연수')
        
        st.line_chart(chart_data)
        st.caption("▲ 적산가액 크기 비교: 상환기금법 > 정액법 > 정률법 (초기 기준)")

# ==============================================================================
# 3. 거래사례비교법 (일괄/구분)
# ==============================================================================
elif menu == "토지건물 일괄/구분 (거래사례비교법)":
    st.header("🏘️ 거래사례비교법")
    
    col_input, col_result = st.columns([1, 1.2])
    
    with col_input:
        st.subheader("1. 사례 및 비교치 입력")
        case_price = st.number_input("사례 가격 (원)", value=800000000, step=1000000, format="%d")
        
        # 사정보정 있음/없음 체크
        is_speculation = st.checkbox("사정보정 필요 (특수사정 개입)", value=True)
        if is_speculation:
            spec_factor = st.number_input("사정보정치", value=0.950, format="%.3f")
        else:
            spec_factor = 1.0
            
        time_factor = st.number_input("시점수정치", value=1.020, format="%.3f")
        reg_factor = st.number_input("지역요인 비교치", value=1.000, format="%.3f")
        ind_factor = st.number_input("개별요인 비교치", value=0.980, format="%.3f")
        area_ratio = st.number_input("면적비교치 (대상/사례)", value=1.100, format="%.3f")
        
    with col_result:
        st.subheader("2. 비준가액 산출")
        
        final_price = case_price * spec_factor * time_factor * reg_factor * ind_factor * area_ratio
        
        st.success(f"🎯 비준가액: {int(final_price):,} 원")
        
        st.markdown("### 📝 수식 풀이")
        st.latex(r'''
        P = P_{case} \times \text{사정} \times \text{시점} \times \text{지역} \times \text{개별} \times \text{면적}
        ''')
        
        st.code(f"""
        {case_price:,} (사례)
        x {spec_factor} (사정보정)
        x {time_factor} (시점수정)
        x {reg_factor} (지역요인)
        x {ind_factor} (개별요인)
        x {area_ratio} (면적비교)
        -----------------------
        = {int(final_price):,} 원
        """)
        
        st.markdown("---")
        st.markdown('<div class="warning-box">', unsafe_allow_html=True)
        st.markdown("#### 💡 핵심 Check Point")
        st.markdown("""
        * **[법규] 감칙 제16조:** 집합건물(아파트, 오피스텔 등)은 거래사례비교법이 주된 방법.
        * **[이론] 배분법:** 일괄 사례 가격에서 토지/건물 가격을 구분해야 할 경우 '비율 방식' 또는 '공제 방식' 사용 고려.
        * **[주의]** 사정보정은 사례가 '정상적인 시장'에서 거래되지 않았을 때만 적용함.
        """)
        st.markdown('</div>', unsafe_allow_html=True)
# ==============================================================================
# 4. 수익성 부동산 (수익환원법 - 자본회수방법별 4방식)
# ==============================================================================
elif menu == "수익성 부동산 (수익환원법)":
    st.header("💰 수익환원법: 자본회수 방법에 따른 분류")
    st.markdown("교과서에 나온 **[직접법 / 직선법 / 상환기금법 / 연금법]** 완벽 비교")

    # 4가지 방법 선택 (교과서 표 순서대로)
    method_option = st.radio(
        "평가 모델을 선택하세요",
        ["1. 직접법 (Direct)", "2. 직선법 (Straight-line)", "3. 상환기금법 (Hoskold)", "4. 연금법 (Inwood)"],
        horizontal=True
    )
    
    st.divider()

    col1, col2 = st.columns([1, 1.3])

    # --- 공통 입력값 ---
    with col1:
        st.subheader("1. 기초 데이터 입력")
        a = st.number_input("순수익 (a, NOI)", value=50000000, step=1000000, format="%d")
        r = st.number_input("자본수익률 (r, 할인율) (%)", value=6.0, step=0.1, format="%.2f") / 100
        
        # 직접법 외에는 내용연수 필요
        if "직접법" not in method_option:
            n = st.number_input("잔존 내용연수 (n년)", value=20, step=1)
        
        # 상환기금법은 안전이율 필요
        if "상환기금법" in method_option:
            i = st.number_input("안전이율/축적이율 (i) (%)", value=2.0, step=0.1, format="%.2f") / 100

    # --- 계산 및 수식 표시 ---
    with col2:
        st.subheader("2. 교과서 수식 및 풀이")

        # -------------------------------------------------------
        # [1] 직접법 (영구환원)
        # -------------------------------------------------------
        if "직접법" in method_option:
            value = a / r
            
            st.latex(r'''
            V = \frac{a}{r}
            ''')
            st.info(f"계산: {int(a):,} ÷ {r} = {int(value):,} 원")
            
            st.markdown("---")
            st.markdown("#### 📖 교과서 이론 Check")
            st.write("✔️ **자본회수:** 불요 (영구적 소득)")
            st.write("✔️ **특징:** 내용연수가 무한한 자산")
            st.error("📍 **적용대상:** 토지, 비상각자산")

        # -------------------------------------------------------
        # [2] 직선법 (내용연수법)
        # -------------------------------------------------------
        elif "직선법" in method_option:
            # 환원율 R = r + 1/n
            cap_recovery = 1 / n
            cap_rate = r + cap_recovery
            value = a / cap_rate
            
            st.latex(r'''
            V = \frac{a}{r + \frac{1}{n}}
            ''')
            
            st.write(f"**· 자본수익률(r):** {r*100:.2f}%")
            st.write(f"**· 자본회수율(1/n):** {cap_recovery*100:.2f}% (직선상각)")
            st.metric("종합 환원율 (R)", f"{cap_rate*100:.2f}%")
            st.success(f"💎 수익가액: {int(value):,} 원")

            st.markdown("---")
            st.markdown("#### 📖 교과서 이론 Check")
            st.write("✔️ **가정:** 매년 일정액 자본회수, **재투자 없음**")
            st.write("✔️ **특징:** 소득이 매년 감소할 것으로 예상되는 자산")
            st.error("📍 **적용대상:** 건물(내용연수 유한), 구축물, 기계")

        # -------------------------------------------------------
        # [3] 상환기금법 (호스콜드)
        # -------------------------------------------------------
        elif "상환기금법" in method_option:
            # 환원율 R = r + i/((1+i)^n - 1)
            sff = i / ((1 + i) ** n - 1)
            cap_rate = r + sff
            value = a / cap_rate
            
            # 교과서 수식 모양 그대로 (분자에 i)
            st.latex(r'''
            V = \frac{a}{r + \frac{i}{(1+i)^n - 1}}
            ''')
            
            st.write(f"**· 자본수익률(r):** {r*100:.2f}% (위험률)")
            st.write(f"**· 상환기금율(SFF @ i):** {sff*100:.4f}% (안전이율 적용)")
            st.metric("종합 환원율 (R)", f"{cap_rate*100:.4f}%")
            st.success(f"💎 수익가액: {int(value):,} 원")
            
            st.markdown("---")
            st.markdown("#### 📖 교과서 이론 Check")
            st.write("✔️ **가정:** 회수된 자본을 **안전이율(i)**로 재투자")
            st.write("✔️ **특징:** 자본회수분이 적게 계산됨 -> 환원율 작음 -> 가액 높게 나올 것 같지만?")
            st.caption("*(주의: 이론상 호스콜드는 위험률 r을 높게 잡으므로 보통 가액이 가장 낮게 평가됨)*")
            st.error("📍 **적용대상:** 광산, 채석장, 광업권 (소멸성 자산)")

        # -------------------------------------------------------
        # [4] 연금법 (인우드)
        # -------------------------------------------------------
        elif "연금법" in method_option:
            # 환원율 R = r + r/((1+r)^n - 1)
            sff = r / ((1 + r) ** n - 1)
            cap_rate = r + sff
            value = a / cap_rate
            
            # 교과서 수식 모양 그대로 (분자에 r)
            st.latex(r'''
            V = \frac{a}{r + \frac{r}{(1+r)^n - 1}}
            ''')
            
            st.write(f"**· 자본수익률(r):** {r*100:.2f}%")
            st.write(f"**· 상환기금율(SFF @ r):** {sff*100:.4f}% (자본수익률 재투자)")
            st.metric("종합 환원율 (R)", f"{cap_rate*100:.4f}%")
            st.success(f"💎 수익가액: {int(value):,} 원")
            
            st.markdown("---")
            st.markdown("#### 📖 교과서 이론 Check")
            st.write("✔️ **가정:** 회수된 자본을 **자본수익률(r)**로 재투자")
            st.write("✔️ **특징:** 수익성 높은 곳에 재투자하므로 가치가 높게 평가됨")
            st.error("📍 **적용대상:** 어업권, 영업권, 임대권 (상각자산)")

# ==============================================================================
# 5. 임대료 평가 (적산법)
# ==============================================================================
elif menu == "임대료 (적산법)":
    st.header("📈 임대료 평가: 적산법")
    
    col1, col2 = st.columns([1, 1.2])
    
    with col1:
        st.subheader("1. 기초데이터 입력")
        base_value = st.number_input("기초가액 (원)", value=500000000, step=1000000, format="%d", help="임대 대상 물건의 원본 가치")
        exp_rate = st.number_input("기대이율 (%)", value=4.0, format="%.2f", step=0.1) / 100
        nec_exp = st.number_input("필요제경비 (원/년)", value=5000000, step=100000, format="%d", help="감가상각비, 유지관리비, 조세공과 등")
        
    with col2:
        st.subheader("2. 적산임료 산출")
        
        base_rent = base_value * exp_rate  # 순임료 상당액
        total_rent = base_rent + nec_exp   # 적산임료
        
        st.success(f"🎯 연간 적산임료: {int(total_rent):,} 원")
        st.info(f"월 환산 임료: {int(total_rent/12):,} 원")
        
        st.markdown("### 📝 수식 풀이")
        st.latex(r'''
        \text{임료} = (\text{기초가액} \times \text{기대이율}) + \text{필요제경비}
        ''')
        
        st.code(f"""
        {int(base_value):,} (기초가액)
        x {exp_rate} (기대이율)
        -----------------------
        = {int(base_rent):,} (순임료)
        + {int(nec_exp):,} (필요제경비)
        -----------------------
        = {int(total_rent):,} 원
        """)

        st.markdown("---")
        st.markdown('<div class="warning-box">', unsafe_allow_html=True)
        st.markdown("#### 💡 핵심 Check Point")
        st.markdown("""
        * **[이론] 기대이율 vs 환원율:** 기대이율은 임대차 기간 동안의 '사용대가' 성격이고, 환원율은 장래 소득을 현재가치로 만드는 '자본화' 성격임.
        * **[법규] 감칙 제22조:** 임대료는 **임대사례비교법**이 원칙. 적산법은 차선책으로 사용하거나 시산가액 조정용으로 씀.
        """)
        st.markdown('</div>', unsafe_allow_html=True)

# ==============================================================================
# 6. 할인현금수지분석법 (DCF) & 자본회수방법 비교
# ==============================================================================
elif menu == "투자타당성/가치 (할인현금수지분석법 DCF)":
    st.header("📉 할인현금수지분석법 및 자본회수방법")
    st.markdown("수익의 흐름과 자본회수 이론에 따른 3가지 평가모델")
    
    # 탭으로 3가지 방법 구분
    dcf_method = st.radio(
        "평가 모델을 선택하세요", 
        ["Type A: 일반 DCF (변동소득)", "Type B: 인우드법 (Inwood)", "Type C: 호스콜드법 (Hoskold)"],
        horizontal=True
    )
    
    st.divider()

    # ---------------------------------------------------
    # Type A: 일반 DCF (기존과 동일 - 변동소득 + 복귀가액)
    # ---------------------------------------------------
    if "Type A" in dcf_method:
        st.subheader("🅰️ 일반 DCF (Standard)")
        st.info("매년 소득이 변동하며, 기말에 부동산을 매각(복귀)하는 일반적 상황")

        col1, col2 = st.columns([1, 1.5])
        with col1:
            holding_period = st.slider("보유기간 (n년)", 1, 10, 5)
            initial_noi = st.number_input("1차년도 순수익 (NOI)", value=50000000, step=1000000, format="%d")
            growth_rate = st.number_input("매년 소득 상승률 (%)", value=2.0, step=0.1, format="%.1f") / 100
            discount_rate = st.number_input("할인율 (Y) (%)", value=7.0, step=0.1, format="%.2f") / 100
            terminal_cap_rate = st.number_input("최종환원율 (R_term) (%)", value=6.0, step=0.1, format="%.2f") / 100
        
        with col2:
            # 계산 로직
            pvs = []
            years = []
            total_pv_noi = 0
            
            for i in range(1, holding_period + 1):
                noi = initial_noi * ((1 + growth_rate) ** (i - 1))
                pv = noi / ((1 + discount_rate) ** i)
                pvs.append(pv)
                years.append(f"{i}년")
                total_pv_noi += pv
            
            next_noi = initial_noi * ((1 + growth_rate) ** holding_period)
            reversion = next_noi / terminal_cap_rate
            reversion_pv = reversion / ((1 + discount_rate) ** holding_period)
            final_value = total_pv_noi + reversion_pv
            
            st.success(f"💎 수익가액: {int(final_value):,} 원")
            
            # 시각화
            chart_data = {"구분": years + ["복귀가액"], "현재가치": [int(p) for p in pvs] + [int(reversion_pv)]}
            st.bar_chart(chart_data, x="구분", y="현재가치")
            
            st.markdown("""
            **💡 특징:** 소득이 매년 변동($g \neq 0$)하고, 자본회수가 보유기간 말 처분(복귀가액)을 통해 이루어짐.
            """)

# ---------------------------------------------------
    # Type B: 인우드법 (Inwood Method) - 환원이율 방식
    # ---------------------------------------------------
    elif "Type B" in dcf_method:
        st.subheader("🅱️ 인우드법 (Inwood Method)")
        st.info("매년 소득이 일정(평준)하며, 회수된 자본을 '할인율(Y)'로 재투자한다고 가정.")

        col1, col2 = st.columns([1, 1.2])
        with col1:
            income = st.number_input("매년 순수익 (A)", value=30000000, step=1000000, format="%d")
            period = st.number_input("수익 기간 (n년)", value=10, step=1)
            rate = st.number_input("자본수익률/할인율 (Y or r) (%)", value=6.0, step=0.1, format="%.2f") / 100
            
        with col2:
            # 현동님이 익숙하신 방식: R = Y + SFF(Y)
            # SFF(Y) = r / ((1+r)^n - 1)
            
            if rate == 0:
                sff_y = 1 / period
            else:
                sff_y = rate / ((1 + rate) ** period - 1)
            
            cap_rate_inwood = rate + sff_y  # 환원이율 R
            
            value = income / cap_rate_inwood
            
            st.metric("상환기금율 (SFF @ Y)", f"{sff_y:.5f}")
            st.metric("종합 환원율 (R)", f"{cap_rate_inwood:.5f}")
            st.success(f"💎 수익가액: {int(value):,} 원")
            
            st.markdown("### 📝 수식 (환원이율 방식)")
            st.latex(r'''
            V = \frac{A}{Y + \frac{Y}{(1+Y)^n - 1}}
            ''')
            st.write("(분모의 뒷부분이 할인율 Y로 구한 상환기금율입니다)")
            
            st.warning("""
            **⚠️ 호스콜드법과의 차이점:**
            * 수식 형태는 $R = Y + SFF$ 로 동일합니다.
            * 다만, 인우드법은 SFF를 구할 때 **할인율(Y)**을 쓰고, 호스콜드법은 **안전이율(i)**을 씁니다.
            """)

    # ---------------------------------------------------
    # Type C: 호스콜드법 (소멸성 자산 + 안전이율)
    # ---------------------------------------------------
    elif "Type C" in dcf_method:
        st.subheader("©️ 호스콜드법 (Hoskold Method)")
        st.info("광산처럼 자산이 소멸되는 경우. 회수된 자본을 매우 안전한 '안전이율(i)'로 재투자한다고 가정.")

        col1, col2 = st.columns([1, 1.2])
        with col1:
            income = st.number_input("매년 순수익 (A)", value=50000000, step=1000000, format="%d")
            period = st.number_input("채굴 가능 연수 (n년)", value=20, step=1)
            spec_rate = st.number_input("위험률/투기적이율 (Y) (%)", value=10.0, step=0.1, format="%.2f", help="해당 사업의 요구수익률") / 100
            safe_rate = st.number_input("안전이율/축적이율 (i) (%)", value=3.0, step=0.1, format="%.2f", help="은행 예금 금리 수준 (국공채 등)") / 100
            
        with col2:
            # 호스콜드 계수
            # Value = A / (Y + SFF_safe)
            # SFF_safe = i / ((1+i)^n - 1)
            
            sff = safe_rate / ((1 + safe_rate) ** period - 1) # 감채기금계수 (안전이율 사용)
            capitalization_rate = spec_rate + sff             # 종합환원율
            
            value = income / capitalization_rate
            
            st.metric("상환기금율 (SFF)", f"{sff:.5f}")
            st.metric("종합 환원율 (R)", f"{capitalization_rate:.5f}")
            st.success(f"💎 수익가액: {int(value):,} 원")
            
            st.markdown("### 📝 수식")
            st.latex(r'''
            V = \frac{A}{Y_{risk} + \frac{i}{(1+i)^n - 1}}
            ''')
            
            st.error("""
            **⚠️ 핵심 차이점 (vs 인우드):**
            * **재투자율:** 인우드는 **할인율(Y)**로 재투자하지만, 호스콜드는 안전한 **안전이율(i)**로 재투자함.
            * **가치 비교:** 보통 안전이율이 할인율보다 낮으므로, 동일 조건 시 **호스콜드법에 의한 가치가 인우드법보다 낮게 나옴** (보수적 평가).
            """)
# ==============================================================================
# 7. 잔여법 & 물리적 투자결합법 (Residual Technique)
# ==============================================================================
elif menu == "토지/건물 가치 분리 (잔여법 & 투자결합법)":
    st.header("🍰 잔여법 & 물리적 투자결합법")
    st.markdown("전체 순수익(NOI)을 토지와 건물이 기여한 몫으로 나누어 가치를 산정합니다.")

    # 탭으로 기능 구분
    method_tab = st.radio("구하려는 가치는?", ["토지 가치 구하기 (토지잔여법)", "건물 가치 구하기 (건물잔여법)"], horizontal=True)
    
    col1, col2 = st.columns([1, 1.5])
    
    with col1:
        st.subheader("1. 조건 입력")
        noi = st.number_input("전체 순영업소득 (NOI)", value=80000000, step=1000000, format="%d")
        
        if "토지" in method_tab:
            st.info("건물의 가치는 원가법 등으로 이미 알고 있다고 가정합니다.")
            known_value = st.number_input("기존 건물 가치 (V_B)", value=500000000, step=1000000, format="%d")
            known_label = "건물"
            target_label = "토지"
        else:
            st.info("토지의 가치는 공시지가법 등으로 이미 알고 있다고 가정합니다.")
            known_value = st.number_input("기존 토지 가치 (V_L)", value=800000000, step=1000000, format="%d")
            known_label = "토지"
            target_label = "건물"

        # 환원율 입력
        st.markdown("##### 환원율 설정")
        r_land = st.number_input("토지 환원율 (R_L) (%)", value=4.0, step=0.1, format="%.2f") / 100
        r_bldg = st.number_input("건물 환원율 (R_B) (%)", value=6.0, step=0.1, format="%.2f", help="보통 감가상각률이 더해져 토지환원율보다 높음") / 100
        
        # 현재 적용할 환원율 결정
        if target_label == "토지":
            target_cap_rate = r_land
            known_cap_rate = r_bldg
        else:
            target_cap_rate = r_bldg
            known_cap_rate = r_land

    with col2:
        st.subheader("2. 가치 산정 및 배분")
        
        # 1) 아는 자산에 귀속되는 소득 계산
        income_known = known_value * known_cap_rate
        
        # 2) 잔여 소득 (모르는 자산의 몫)
        income_residual = noi - income_known
        
        # 3) 모르는 자산의 가치 환원
        if income_residual < 0:
            st.error("⚠️ 에러: 잔여 소득이 마이너스입니다! 입력한 자산 가치가 너무 높거나 환원율이 과대합니다.")
            value_target = 0
        else:
            value_target = income_residual / target_cap_rate
            
        total_value = known_value + value_target
        weighted_r = (value_target/total_value * target_cap_rate) + (known_value/total_value * known_cap_rate)

        # 결과 출력
        st.metric(f"구해진 {target_label} 가치", f"{int(value_target):,} 원")
        st.success(f"🏘️ 전체 부동산 가치: {int(total_value):,} 원")
        st.caption(f"(종합 환원율: {weighted_r*100:.2f}%)")

        # 시각화 (도넛 차트)
        import pandas as pd
        
        st.markdown("### 📊 소득의 배분 (Income Split)")
        
        # 데이터프레임 생성 (스트림릿 차트용)
        df_income = pd.DataFrame({
            "Source": [f"{known_label} 귀속 소득", f"{target_label} 귀속 소득 (잔여)"],
            "Amount": [income_known, income_residual]
        })
        
        # 막대 차트로 비중 보여주기
        st.bar_chart(df_income, x="Source", y="Amount")
        
        st.markdown("### 📝 수식 풀이")
        st.latex(r'''
        V_{target} = \frac{NOI - (V_{known} \times R_{known})}{R_{target}}
        ''')
        
        st.code(f"""
        {int(noi):,} (전체 NOI)
        - {int(income_known):,} ({known_label} 몫: {int(known_value):,} x {known_cap_rate*100}%)
        ---------------------------
        = {int(income_residual):,} ({target_label} 귀속 잔여소득)
        ÷ {target_cap_rate} ({target_label} 환원율)
        ---------------------------
        = {int(value_target):,} 원 ({target_label} 가치)
        """)
        
        st.markdown("---")
        st.markdown('<div class="info-box">', unsafe_allow_html=True)
        st.markdown("#### 💡 핵심 Check Point")
        st.markdown("""
        * **[이론] 물리적 투자결합법:** 토지와 건물의 소득창출능력이 다르고, 위험(환원율)도 다르다는 가정에서 출발함.
        * **[공식] $R_B > R_L$:** 건물은 영속적이지 않고 감가상각이 되므로, 자본회수분이 포함된 건물환원율이 토지환원율보다 높은 것이 일반적임.
        * **[실무]** 토지잔여법은 건물이 신축(최유효이용)일 때 가장 정확하며, 오래된 건물일수록 철거비 등의 변수로 인해 신뢰도가 낮아짐.
        """)
        st.markdown('</div>', unsafe_allow_html=True)
# ==============================================================================
# 8. 임대료 평가 (임대사례비교법 - 원칙)
# ==============================================================================
elif menu == "임대료 (임대사례비교법)":
    st.header("🏙️ 임대료 평가: 임대사례비교법 (원칙)")
    st.markdown("사례의 실질임대료를 산정하여 대상 물건의 적정 임대료를 도출합니다.")
    
    col1, col2 = st.columns([1, 1.3])
    
    with col1:
        st.subheader("1. 임대사례 데이터")
        # 사례의 계약 내용
        case_deposit = st.number_input("사례 보증금 (원)", value=100000000, step=1000000, format="%d")
        case_monthly = st.number_input("사례 월세 (원/월)", value=3000000, step=100000, format="%d")
        case_maintenance = st.number_input("사례 관리비 등 (실비초과분만) (원/월)", value=0, step=10000, format="%d", help="실비상당액은 제외하고, 실질적 임대료 성격인 부분만 입력")
        
        st.markdown("##### ⚙️ 보증금 운용이율 (전환율)")
        conversion_rate = st.number_input("보증금 적용 이율 (%)", value=5.0, step=0.1, format="%.2f", help="보증금을 월세로 전환하거나 운용익을 계산할 때 적용") / 100
        
        st.divider()
        st.subheader("2. 비교치 입력")
        spec_check = st.checkbox("사정보정 필요 (특수관계 거래 등)", value=False)
        spec_factor = 1.0
        if spec_check:
            spec_factor = st.number_input("사정보정치", value=1.000, format="%.3f")
            
        time_factor = st.number_input("시점수정치", value=1.020, format="%.3f")
        reg_factor = st.number_input("지역요인 비교치", value=1.000, format="%.3f")
        ind_factor = st.number_input("개별요인 비교치", value=1.050, format="%.3f")
        area_ratio = st.number_input("면적 비교치 (대상/사례)", value=1.000, format="%.3f")

    with col2:
        st.subheader("3. 비준임료 산출")
        
        # [Step 1] 사례의 실질임료 산정 (연간 기준)
        # 운용익 = 보증금 * 이율
        deposit_income = case_deposit * conversion_rate
        # 연간 월세 = 월세 * 12
        annual_monthly_rent = case_monthly * 12
        # 연간 관리비 수입 = 관리비 * 12
        annual_maintenance = case_maintenance * 12
        
        # 사례 실질임료 합계
        case_real_rent = deposit_income + annual_monthly_rent + annual_maintenance
        
        # [Step 2] 비준임료 계산
        target_rent = case_real_rent * spec_factor * time_factor * reg_factor * ind_factor * area_ratio
        
        # 결과 출력
        st.markdown(f"**① 사례 보증금 운용익:** {int(deposit_income):,} 원")
        st.markdown(f"**② 사례 연간 월세:** {int(annual_monthly_rent):,} 원")
        st.info(f"👉 **사례 연간 실질임료:** {int(case_real_rent):,} 원 (①+②+관리비)")
        
        st.divider()
        st.success(f"🎯 대상 물건 연간 비준임료: {int(target_rent):,} 원")
        st.caption(f"월 환산액: {int(target_rent/12):,} 원")
        
        st.markdown("### 📝 수식 풀이")
        st.latex(r'''
        \text{임료} = \text{사례실질임료} \times \text{사정} \times \text{시점} \times \text{지역} \times \text{개별} \times \text{면적}
        ''')
        
        st.code(f"""
        {int(case_real_rent):,} (사례)
        x {spec_factor} (사정)
        x {time_factor} (시점)
        x {reg_factor} (지역)
        x {ind_factor} (개별)
        x {area_ratio} (면적)
        -----------------------
        = {int(target_rent):,} 원
        """)
        
        st.markdown("---")
        st.markdown('<div class="warning-box">', unsafe_allow_html=True)
        st.markdown("#### 💡 핵심 Check Point")
        st.markdown("""
        * **[법규] 감칙 제22조:** 임대료를 감정평가할 때는 **임대사례비교법**을 주된 방법으로 적용하여야 한다. (적산법은 예외적)
        * **[실무] 실질임료 구성:** 지불임료(월세) + 예금적 성격의 일시금(보증금)의 운용익 + 실비초과 관리비 등을 모두 합산해야 함.
        * **[팁]** 층별 효용비(개별요인)가 임대료 격차에 가장 큰 영향을 미침. (1층 vs 2층)
        """)
        st.markdown('</div>', unsafe_allow_html=True)
# ==============================================================================
# 9. 공공사업 보상 (보상평가)
# ==============================================================================
elif menu == "공공사업 보상 (보상평가)":
    st.header("🚧 보상평가: 토지보상법 적용")
    st.markdown("개발이익을 배제한 '적정가격' 산정 (사업인정고시일 기준)")
    
    col1, col2 = st.columns([1, 1.3])
    
    with col1:
        st.subheader("1. 기준시점 및 표준지 확정")
        # 날짜 입력 기능
        project_date = st.date_input("사업인정 고시일", datetime.date(2025, 1, 15))
        std_year = st.selectbox("적용할 표준지 공시년도", [project_date.year, project_date.year-1])
        
        st.info(f"💡 원칙적으로 **{project_date.year}년 1월 1일** 공시지가를 기준으로 하되, 토지보상법 제70조에 따라 사업인정 고시일 전의 시점을 적용해야 합니다.")

        st.subheader("2. 가액 산정 요인")
        std_price = st.number_input("비교표준지 공시지가 (원/㎡)", value=2000000, step=10000, format="%d")
        
        # 보상평가의 핵심: 시점수정
        st.markdown("##### ⓐ 시점수정 (지가변동률)")
        time_factor = st.number_input("시점수정치", value=1.015, format="%.4f", help="사업인정고시일 전의 표준지 공시기준일부터 가격시점까지의 변동률 (해당 사업 영향 배제)")
        
        reg_factor = st.number_input("지역요인 (표준지 vs 대상)", value=1.000, format="%.3f")
        ind_factor = st.number_input("개별요인 (가로/접근/획지 등)", value=1.100, format="%.3f")
        other_factor = st.number_input("그 밖의 요인 (기타요인)", value=1.350, format="%.3f", help="인근 보상선례 등을 참작하되 개발이익은 배제")
        
        area = st.number_input("편입 면적 (㎡)", value=330.0, format="%.1f")

    with col2:
        st.subheader("3. 보상액 산출")
        
        unit_price = std_price * time_factor * reg_factor * ind_factor * other_factor
        total_comp = unit_price * area
        
        st.success(f"💰 토지 보상평가액: {int(total_comp):,} 원")
        st.caption(f"(단가: {int(unit_price):,} 원/㎡)")
        
        st.markdown("### 📝 보상평가 산식")
        st.latex(r'''
        P_{comp} = P_{std} \times \text{시점(배제)} \times \text{지역} \times \text{개별} \times \text{그밖}
        ''')
        
        st.code(f"""
        [적용 표준지: {std_year}년도]
        {std_price:,} 
        x {time_factor} (시점수정: 개발이익 배제)
        x {reg_factor} (지역요인)
        x {ind_factor} (개별요인)
        x {other_factor} (그 밖의 요인)
        -----------------------
        = {int(unit_price):,} 원/㎡
        """)
        
        st.markdown("---")
        st.markdown('<div class="warning-box">', unsafe_allow_html=True)
        st.markdown("#### 💡 핵심 Check Point")
        st.markdown("""
        * **[법규] 토지보상법 제70조:** 보상액은 **사업인정고시일 전**의 협의 성립 당시 또는 수용재결 당시의 공시지가를 기준으로 함.
        * **[이론] 개발이익 배제:** 해당 공익사업으로 인한 지가 상승분은 철저히 배제해야 하므로, 시점수정 시 해당 사업과 관계없는 인근 시군구의 지가변동률을 씀.
        * **[실무] 그 밖의 요인:** 보상평가에서 가장 논란이 되는 부분. 인근의 정상적인 거래사례나 보상선례를 기준으로 하되, 그 선례에 개발이익이 포함되었는지 검토 필요.
        """)
        st.markdown('</div>', unsafe_allow_html=True)

# ==============================================================================
# 10. 재개발/재건축 (정비사업평가)
# ==============================================================================
elif menu == "재개발/재건축 (정비사업평가)":
    st.header("🏗️ 정비사업평가: 종전자산 & 분담금")
    st.markdown("비례율(%) 산정을 통해 조합원의 권리가액과 분담금을 예측합니다.")

    col1, col2 = st.columns([1, 1.2])

    with col1:
        st.subheader("1. 사업 전체 개요 (조합)")
        total_revenue = st.number_input("총 수입 추산액 (종후자산 총액) (억 원)", value=5000, step=10, help="아파트/상가 일반분양 + 조합원분양 수입 합계") * 100000000
        total_cost = st.number_input("총 사업비 추산액 (공사비 등) (억 원)", value=3500, step=10) * 100000000
        total_before_asset = st.number_input("종전자산 평가액 총합 (억 원)", value=1200, step=10, help="조합원들이 가진 기존 토지/건물의 감정평가액 합계") * 100000000
        
        st.divider()
        st.subheader("2. 나의 자산 (개별 조합원)")
        my_appraisal = st.number_input("내 종전자산 평가액 (원)", value=800000000, step=10000000, format="%d", help="감정평가사가 평가한 내 헌 집 가격")
        target_apt_price = st.number_input("입주희망 평형 조합원분양가 (원)", value=1100000000, step=10000000, format="%d")

    with col2:
        st.subheader("3. 결과 분석: 분담금 vs 환급금")
        
        # [Step 1] 비례율 계산
        # 비례율 = (총수입 - 총비용) / 종전자산총액
        if total_before_asset == 0:
            proportional_rate = 0
        else:
            proportional_rate = (total_revenue - total_cost) / total_before_asset
        
        # [Step 2] 권리가액 계산
        my_rights_value = my_appraisal * proportional_rate
        
        # [Step 3] 분담금 계산 (분양가 - 권리가액)
        contribution = target_apt_price - my_rights_value
        
        # 결과 시각화
        st.metric("📊 비례율 (사업성 지표)", f"{proportional_rate*100:.2f} %")
        if proportional_rate > 1.0:
            st.caption("▲ 100% 초과: 사업성이 우수함 (권리가액 증가)")
        else:
            st.caption("▼ 100% 미만: 사업성이 낮음 (권리가액 감소)")
            
        st.divider()
        st.markdown(f"**🏡 내 종전자산 평가액:** {int(my_appraisal):,} 원")
        st.markdown(f"**➕ 비례율 적용 권리가액:** **{int(my_rights_value):,} 원**")
        
        if contribution > 0:
            st.error(f"💸 납부할 분담금: {int(contribution):,} 원")
        else:
            st.success(f"💰 돌려받을 환급금: {int(abs(contribution)):,} 원")
            
        st.markdown("### 📝 수식 풀이")
        st.latex(r'''
        \text{비례율} = \frac{\text{총수입} - \text{총사업비}}{\text{종전자산평가총액}} \times 100
        ''')
        st.latex(r'''
        \text{분담금} = \text{조합원분양가} - (\text{내 종전자산} \times \text{비례율})
        ''')

        st.markdown("---")
        st.markdown('<div class="info-box">', unsafe_allow_html=True)
        st.markdown("#### 💡 핵심 Check Point")
        st.markdown("""
        * **[법규] 도시정비법:** 관리처분계획 수립 시 종전자산 가격은 '사업시행인가 고시일'이 기준임.
        * **[실무] 비례율 조정:** 조합은 비례율을 100% 근처로 맞추기 위해 공사비(비용)나 일반분양가(수입)를 조정하기도 함.
        * **[용어] 권리가액:** 조합원이 실제 재산으로 인정받는 금액. (감정평가액이 아님! 감평액에 비례율을 곱해야 함)
        """)
        st.markdown('</div>', unsafe_allow_html=True)
# ==============================================================================
# 11. 산정 요인 및 고려사항 가이드 (Reference)
# ==============================================================================
elif menu == "📋 산정 요인 및 고려사항 가이드":
    st.header("📋 감정평가 주요 산정 요인 및 고려사항")
    st.markdown("물건별·목적별로 가격 형성에 영향을 미치는 핵심 요인(Checklist)입니다.")

    # 탭으로 구분하여 정리
    tab1, tab2, tab3, tab4, tab5 = st.tabs(["토지 (Land)", "건물 (Building)", "집합건물 (Apt)", "임대료/권리", "목적별 특이사항"])

    # --------------------------------------------------------------------------
    # Tab 1: 토지 (공시지가기준법/거래사례비교법)
    # --------------------------------------------------------------------------
    with tab1:
        st.subheader("🌲 토지 평가 시 고려 요인")
        st.info("토지는 '용도지역'이 가치를 결정하는 가장 큰 요인이며, 물리적 현황에 따른 격차가 큽니다.")
        
        st.markdown("""
        | 구분 | 주요 확인 사항 (개별요인) | 세부 내용 및 용어 |
        | :--- | :--- | :--- |
        | **가로조건**<br>(도로) | **도로 폭 & 접면** | - **광대로/중로/소로:** 도로 폭에 따른 분류<br>- **세로(가):** 자동차 통행 가능<br>- **세로(불):** 자동차 통행 불가<br>- **맹지:** 도로에 접하지 않음 (가치 감가 심함) |
        | **접면가로** | **접함 상태** | - **한면/각지:** 코너(각지) 토지는 상업적 가치가 높음<br>- **3면/4면접:** 개방감이 좋으나 프라이버시 침해 가능성 |
        | **접근조건** | **교통/편의시설** | - **역세권:** 지하철역 거리 (도보 5분/10분)<br>- **공공시설:** 시청, 학교 등과의 거리<br>- **혐오시설:** 쓰레기장, 묘지 등 접근성 (감가요인) |
        | **환경조건** | **자연/물리적 환경** | - **고저:** 평지, 완경사, 급경사, 저지(침수우려)<br>- **형상:** 정방형(네모반듯), 장방형, 부정형(못생김)<br>- **방위:** 남향, 동향 등 (주거용에서 중요) |
        | **획지조건** | **이용 상태** | - **면적:** 표준적 면적 대비 과소/과대 여부<br>- **용도:** 나지(빈 땅), 건부지(건물 있음) |
        | **행정조건** | **공법상 제한** | - **용도지역:** 주거/상업/공업/녹지 (건폐율/용적률 결정)<br>- **제한구역:** 개발제한구역(Greenbelt), 문화재보호구역 |
        """, unsafe_allow_html=True)
        st.warning("⚠️ **비교표준지 선정 시:** 용도지역과 이용상황이 동일/유사한 것을 선정하는 것이 제1원칙입니다.")

    # --------------------------------------------------------------------------
    # Tab 2: 건물 (원가법)
    # --------------------------------------------------------------------------
    with tab2:
        st.subheader("🏗️ 건물 평가 시 고려 요인")
        st.info("건물은 '재조달원가'와 '감가수정(상태)'을 파악하는 것이 핵심입니다.")
        
        st.markdown("""
        | 구분 | 주요 확인 사항 | 세부 내용 |
        | :--- | :--- | :--- |
        | **구조/용도** | **내구연한 결정** | - **구조:** 철근콘크리트(RC), 철골조, 조적조(벽돌), 목조<br>- **용도:** 주거용, 상업용, 공업용 (내용연수가 다름) |
        | **시공상태** | **마감 및 품질** | - **외부마감:** 석재, 타일, 페인트, 유리커튼월<br>- **내부마감:** 바닥재, 천장재, 인테리어 수준 |
        | **설비내역** | **부대설비** | - **위생설비:** 급배수, 화장실 상태<br>- **냉난방:** 중앙공급 vs 개별난방, 시스템에어컨 유무<br>- **기타:** 승강기(E/V), 소화전, 홈오토메이션 |
        | **관리상태** | **감가수정 (관찰)** | - **물리적 감가:** 균열, 누수, 파손, 노후화 정도<br>- **기능적 감가:** 구식 평면설계, 주차장 협소, E/V 부족<br>- **경제적 감가:** 인근 상권 쇠퇴, 건물과 부지의 부적합 |
        | **부대건물** | **종물/부합물** | - 창고, 차고, 담장, 대문 등의 포함 평가 여부 |
        """, unsafe_allow_html=True)

    # --------------------------------------------------------------------------
    # Tab 3: 집합건물 (거래사례비교법)
    # --------------------------------------------------------------------------
    with tab3:
        st.subheader("🏢 아파트/오피스텔 평가 시 고려 요인")
        st.info("거래사례비교법 적용 시 '사례와의 격차'를 보정하는 것이 핵심입니다.")
        
        st.markdown("""
        | 구분 | 주요 확인 사항 (격차율) | 세부 내용 |
        | :--- | :--- | :--- |
        | **층별효용** | **층수에 따른 가격차** | - **로얄층:** 조망/일조가 좋은 중상층부 (기준 100)<br>- **저층:** 1층(사생활), 2~3층 (감가 요인)<br>- **탑층:** 옥상 누수 우려 vs 층간소음 없음 (선호도 갈림) |
        | **호별효용** | **라인/위치** | - **향:** 남향 > 남동/남서 > 동향 > 서향 > 북향<br>- **위치:** 엘리베이터 인접 소음, 복도식 vs 계단식 |
        | **단지특성** | **브랜드/규모** | - **브랜드:** 1군 건설사 vs 중소 건설사<br>- **세대수:** 대단지(1000세대↑)가 커뮤니티/관리비 유리<br>- **주차:** 세대당 주차대수, 지하주차장 연결 여부 |
        | **입지여건** | **학군/교통** | - **초품아:** 초등학교를 품은 아파트 (가치 상승)<br>- **역세권:** 지하철역 도보 거리<br>- **편의:** 대형마트, 백화점, 공원 인접성 |
        """, unsafe_allow_html=True)
        st.warning("⚠️ **실무 Tip:** 같은 아파트 단지라도 '동'의 위치(단지 입구, 뷰)에 따라 가격 차이가 10% 이상 나기도 합니다.")

    # --------------------------------------------------------------------------
    # Tab 4: 임대료 및 무형자산 (수익방식)
    # --------------------------------------------------------------------------
    with tab4:
        st.subheader("💰 임대료 및 권리 평가 시 고려 요인")
        
        col_r1, col_r2 = st.columns(2)
        
        with col_r1:
            st.markdown("**1. 임대료 평가 (임대사례비교법)**")
            st.markdown("""
            * **계약 내용:** 보증금/월세 비율, 전세 전환율, 계약 기간
            * **임대 조건:** 관리비 포함 내역(청소/경비/공과금)
            * **층별 효용:** 1층(접근성 최고) vs 2층 이상(급격한 감가)
            * **전용률:** 실제 쓸 수 있는 면적 비율
            """)
            
        with col_r2:
            st.markdown("**2. 영업권/기업가치 (수익환원법)**")
            st.markdown("""
            * **수익성:** 과거 3~5년 매출액 및 영업이익 추이
            * **지속성:** 초과수익이 얼마나 유지될 것인가 (법적 보호 등)
            * **위험성:** 경쟁 업체 진입 가능성, 시장 변화 (할인율 반영)
            * **무형요인:** 브랜드 인지도, 거래처 관계, 특허 기술
            """)

    # --------------------------------------------------------------------------
    # Tab 5: 목적별 특이사항 (담보/경매/보상)
    # --------------------------------------------------------------------------
    with tab5:
        st.subheader("⚖️ 평가 목적에 따른 특별 고려사항")
        st.markdown("동일한 물건이라도 **'평가 목적'**에 따라 중요하게 보는 포인트가 다릅니다.")
        
        st.markdown("""
        | 평가 목적 | 핵심 고려사항 (Key Point) | 평가 방향 (Tendency) |
        | :--- | :--- | :--- |
        | **담보 평가**<br>(은행 대출) | **환가성(현금화)**<br>- 경매로 넘어갔을 때 잘 팔릴 물건인가?<br>- 유치권, 법정지상권 등 권리하자 유무 | **보수적 평가**<br>(떼일 돈 걱정) |
        | **경매 평가**<br>(법원) | **권리분석 & 점유관계**<br>- 대항력 있는 임차인, 밀린 관리비<br>- 공부상 용도와 현황 용도의 차이 (위반건축물) | **시장가치 평가**<br>(낙찰 예상가 아님!) |
        | **보상 평가**<br>(수용) | **객관적 현황 & 개발이익 배제**<br>- 불법형질변경 토지는 변경 전 상태로 평가<br>- 무허가건축물은 적법할 때보다 감액 | **적정가격 평가**<br>(법적 기준 준수) |
        | **일반 거래**<br>(매매/시세) | **최유효이용 & 시장선호도**<br>- 현재 가장 비싸게 팔릴 수 있는 이용 방법<br>- 인테리어, 뷰, 로얄층 프리미엄 적극 반영 | **시장가치 평가**<br>(실거래가 근접) |
        """, unsafe_allow_html=True)
        
        st.error("""
        **🛑 필수 체크 (특수 조건):**
        * **맹지(도로 없음):** 인접 토지 합병 없이는 건축 불가 → 감가율 30% 이상 적용.
        * **완충녹지/접도구역:** 건축 행위 제한 → 토지 가치 급락.
        * **법정지상권 성립여부:** 토지 사용에 제한이 걸림 → 토지 가치 하락.
        """)